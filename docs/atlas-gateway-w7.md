# Atlas Gateway W7

Private review route: `/internal/atlas-gateway`. It is no-indexed, candidate-gated, absent publicly, and leaves `/start-a-project` unchanged.

The Ogun Gateway offers seven verified or carefully framed entry routes: websites, e-commerce/business systems, publishing/OJS, infrastructure/support, branding/design, integrations/automation, and uncertain needs. `ProjectRouteSelector` is keyboard and touch accessible, uses `aria-pressed`, holds selection only in React memory, writes no storage, and sends no request.

The readiness sequence is route, context, then human review. W7 creates no lead, proposal, booking, reference, or success state. Existing public project brief and contact routes remain the only onward actions.

`ILL-0080` is a fictional candidate pavilion, not a transport facility, office, client record, or government endorsement. Evidence: [desktop](../output/playwright/atlas-gateway-w7/desktop-opening-1440x1000.png), [mobile](../output/playwright/atlas-gateway-w7/mobile-opening-390x844.png), [reduced motion](../output/playwright/atlas-gateway-w7/reduced-motion-opening-1440x1000.png).
