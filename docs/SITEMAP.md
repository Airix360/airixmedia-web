# Sitemap

Implemented route families:

- Core: `/`, `/services`, five practice routes
- Publishing: `/publishing`, `/publishing/ojs`, universities, platforms, hosting, editorial support, plugins, pricing
- Proof: `/systems`, `/systems/[slug]`, `/open-source`, `/open-source/[slug]`
- Content: `/insights`, article template, `/resources`, resource template
- Company: `/company`, `/partners`, `/careers`
- Trust: `/support`, `/support/emergency`, `/knowledge-base`, `/status`, `/security`, `/service-levels`, `/legal`
- Conversion: `/start-a-project`, `/contact`
- Locales: `/fr` and `/pt` critical journeys
