import type { Metadata } from "next";
export const metadata:Metadata={title:"Atlas Trust W6 — Internal Review",description:"Private visual and implementation review of the Edo Trust identity.",robots:{index:false,follow:false,noarchive:true,nosnippet:true}};
export default function Layout({children}:{children:React.ReactNode}){return children}
