import type { Metadata } from "next";

export const metadata: Metadata = { title: "Atlas artwork R2 · Owner review", robots: { index: false, follow: false, nocache: true, noarchive: true } };

export default function Layout({ children }: { children: React.ReactNode }) { return children; }
