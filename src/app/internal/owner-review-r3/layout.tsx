import type { Metadata } from "next";
export const metadata: Metadata = { title: "Atlas R3 Wave 1 · Owner review", robots: { index: false, follow: false, nocache: true, noarchive: true, noimageindex: true } };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
