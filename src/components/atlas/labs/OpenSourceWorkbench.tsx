import { experimentRoute } from "@/content/atlas/labs";import styles from "./atlas-labs.module.css";
export function OpenSourceWorkbench(){return <section className={styles.route} aria-labelledby="route-title"><header><span>Engineering route · proposed structural model</span><h2 id="route-title">Problem to maintenance, without skipping the record.</h2><p>The route describes a disciplined Labs rhythm. It is not a roadmap, delivery promise, or universal release process.</p></header><ol data-experiment-route>{experimentRoute.map(([index,name,detail])=><li key={index}><span>{index}</span><i aria-hidden="true"/><h3>{name}</h3><p>{detail}</p></li>)}</ol><p className={styles.routeEquivalent}><strong>Textual equivalent:</strong> identify the problem, create an inspectable prototype, test behaviour and compatibility, document the constraints, publish only a supported release state, and keep maintenance status and limitations visible.</p></section>}
